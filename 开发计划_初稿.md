# 「拍够了吗？」软件开发与使用计划 v2.0

> 2026-09-23 更新。本版弃用 X5 相机连接、目录扫描和自动同步，正式产品只分析用户主动选择的本地视频。
> 产品以用户预先填写的成片目标和确认后的分镜清单为判断基准：系统分析本地视频对目标分镜的覆盖情况，定位有效证据，并给出缺镜或补拍建议。
>
> 当前实现已具备项目、分镜、单文件本地导入、媒体处理、模型分析、证据匹配和覆盖规则。实施重点是将本地导入从备用入口提升为主流程，移除产品对相机会话的依赖，并补齐多文件导入、完成状态和验收。

## 1. 文档入口和实施边界

- [架构与模块接口](docs/架构与接口设计.md)：现有进程、数据、模块职责、服务接口和状态语义；实施时同步改写相机主链路。
- [开发测试与验收流程](docs/开发测试与验收流程.md)：现有测试分层和用例；实施时将真机及同步用例替换为本地导入验收。
- [交接说明](docs/交接说明.md)：现有实现、已验证能力和历史 X5 探测结论。
- 本文件：新产品范围、实施顺序、任务卡和最终使用流程。

本计划只授权后续实施范围，本次不改代码、配置、接口契约或其他文档。后续开发应按任务卡逐项执行，并同步更新受影响的架构、测试、README 和契约。

计划包括 Mac 本地运行、项目与分镜管理、本地视频导入、媒体处理、视觉分析、覆盖判断、补拍建议、证据回看、持久化与故障恢复。不包括相机连接、相机控制、相机目录浏览、录制中新片发现和设备同步。

## 2. 产品目标与使用流程

产品回答的问题是：针对用户想拍出的短视频，现有本地视频是否覆盖了预先确认的必要内容；如果没有，下一段最值得补拍什么。产品不预设成片一定是教程，也适用于探店、Vlog、开箱和产品展示等目标。

1. 用户创建拍摄项目，输入成片目标、目标成片时长与拍摄条件。
2. 软件生成分镜草稿。每个分镜包含可观察、可验证的通过标准；用户修改并确认后，将其锁定为分析基准。
3. 用户选择一个或多个本地视频。界面在提交前展示文件名、大小和数量，导入后逐个显示校验、转码、分析和失败状态。
4. 后端安全保存导入文件，使用 ffprobe/FFmpeg 完整验证视频、生成浏览器可播放的分析副本，并在需要时分段，同时保留源视频时间映射。
5. 模型观察视频，将可见动作、结果和质量问题匹配到已确认的分镜标准。模型只产生候选证据，规则模块负责最终覆盖判断。
6. 页面展示分镜进度、本地素材与证据、当前建议。点击证据后，在分析所用的视频副本中跳转到对应时间段。
7. 若必要标准未满足，系统给出一条最高优先级建议，说明拍什么、怎么拍和为什么；若存在遮挡、模糊或动作不完整，则明确建议补拍。
8. 所有已纳入视频处理完成且必要分镜均有合格证据时，显示“分镜覆盖完整”；若仍有视频在处理或失败，则显示“正在分析”或明确错误，不依赖相机扫描状态。
9. 用户可继续导入视频，或排除误选素材。素材归属变化后，证据和覆盖结果按当前有效素材重新计算。
10. 重新打开项目后，分镜、素材、处理状态、证据和建议均可恢复。

页面保留三个主要区块：分镜进度、素材与证据、当前建议。顶部状态改为本地视频处理和模型状态，不再显示相机连接、目录扫描或同步会话。

## 3. 首版范围与成功定义

### 必须交付

- Mac 本地运行：React + TypeScript + Vite，Python + FastAPI，SQLite，本地媒体存储，FFmpeg/ffprobe。
- 多项目持久保存；每个项目以确认后的成片目标和分镜清单作为分析基准。
- 一个或多个本地视频的批量选择、逐文件导入、内容去重、格式校验、分析副本和时间映射。
- 导入 → 媒体处理 → 视频观察 → 分镜匹配 → 规则重算 → 单条补拍建议的完整链路。
- 多对多证据：一个分镜可由多段视频共同证明，一段视频可覆盖多个分镜。
- 素材播放器和证据定位；排除、重新纳入和失败重试后正确重算。
- 模型上传授权、结构化输出校验、持久任务、重启恢复、资源限制和明确错误提示。
- 不依赖相机或同步会话的完成状态：全部有效视频处理完成且必要分镜覆盖齐全时显示“分镜覆盖完整”。
- 安装、配置、运行、使用和故障排查文档。

### 明确移出范围

- X5 或其他相机连接、目录扫描、下载、增量发现、同步恢复、设备断连和存储卡会话。
- 相机录制控制、实时视频流分析、录制过程中自动分析。
- 自动剪辑、时间线生成、成片导出、多人协作、账户支付和公网部署。
- 未经预处理的任意全景、双鱼眼或专有多镜头原片自动分析。
- 仅上传视频后让模型猜测成片目标并生成剪辑结构。首版必须先有用户确认的目标和分镜清单。

30—60 秒是目标成片的规划范围，不是导入视频长度限制。单文件大小、磁盘空间、处理时长和模型输入上限分别配置。较长视频可保留原文件并生成分段分析副本；不得只分析前半段却把整段标为完成。

历史 X5 探测代码、测试证据和文档可在迁移期保留归档，但不得继续出现在正式产品流程、默认配置、完成判断或交付验收中。删除历史代码前须检查引用关系和数据兼容性。

## 4. 技术原则与关键决策

### 4.1 分镜先于视频分析

视频分析必须围绕用户确认后的分镜标准进行。规划模型负责把成片目标拆成可观察标准，且不默认目标是教程；视频模型负责报告在什么时间段看到了什么；规则模块负责判断标准是否满足。模型不能根据视频内容临时改变完成标准。

已确认的分镜清单在当前项目版本内保持锁定。未来若支持修改，应创建新清单版本并重新匹配旧证据，不能直接沿用旧覆盖结论。

### 4.2 本地视频是一等素材来源

每次导入产生独立 Clip，记录原始文件名、内容哈希、大小、导入时间、处理状态和本地来源。同一项目内内容哈希相同的视频不得重复产生有效分析任务。文件名只用于展示，不能用于推断内容或作为唯一身份。

浏览器上传是首版入口。后端只接收文件内容，不接受前端提供的任意本地路径。临时文件写入受控数据目录，成功后原子转移；失败或重复导入产生的无引用临时文件应清理。

首版支持普通视角视频。投影未知时不得默认视为普通视角；界面要求用户确认，媒体服务仍须验证视频轨、时长和完整可解码性。

### 4.3 完成状态只依赖当前项目数据

- `not_ready`：所有当前素材均已处理，但至少一个必要分镜标准缺少合格证据；未导入素材也属于此状态。
- `checking`：存在已纳入且尚未分析完成的视频，或存在需要重试、排除或处理的阻塞失败。
- `ready`：所有当前纳入的视频均已分析完成、证据文件存在、没有阻塞失败，且全部必要分镜标准已覆盖。

内部枚举名可在迁移期保留以降低风险，界面文案改为“还缺一些镜头”“正在分析”“分镜覆盖完整”。原有 `unverified` 相机新鲜度语义退出正式流程；若为旧数据兼容暂时保留，纯本地项目不得正常进入该状态。

完成时间取当前有效素材最后一次分析或覆盖重算完成时间，不再使用目录扫描时间，也不返回相机快照作为完成依据。

### 4.4 可解释结果和保守判定

每条支持、缺陷或不确定证据必须关联 Clip、分析副本和合法时间范围。需要连续动作的标准必须由连续时间段证明；多个互不相干的静帧不能拼成连续操作证据。

模型结构合法不等于视觉判断正确。完成结论由确定性规则生成；不确定候选不能计入覆盖。已有合格证据不会因新导入的差视频被撤销，但排除提供该证据的素材后必须立即回退。

## 5. 实施顺序和任务卡

本版任务编号重新定义。开始编码前先读取当前实现和交接文档，保护已有数据与无关功能。每项交付均须报告修改文件、测试结果、迁移影响和未验证项。

| 阶段 | 内容 | 出口 |
|---|---|---|
| A | L00 现状与迁移基线；L01 契约及状态重构 | 冻结本地视频主流程和兼容边界 |
| B | L02 本地导入；L03 媒体处理；L04 规划与分析 | 多视频稳定进入结构化证据链路 |
| C | L05 覆盖与建议；L06 前端流程；L07 集成恢复 | 从项目创建到补拍建议完整走通 |
| D | L08 验收与迁移清理；L09 运行交付 | 无相机依赖的版本可复现交付 |

### L00：现状盘点与迁移基线

- 工作：列出相机相关 API、Schema、worker、状态字段、前端入口、测试和文档引用；确认现有本地导入管线的可复用部分；建立旧项目读取样例。
- 交付：迁移清单、受影响接口表、旧数据兼容策略和新验收矩阵草案。
- 验收：明确区分可复用、需改写、仅归档和可删除的内容；未确认引用关系前不删除相机模块。
- 责任范围：只读盘点及设计文档。依赖：无。

### L01：本地流程契约与状态重构

- 工作：定义本地素材来源、批量导入响应、逐文件状态、项目完成时间和本地 readiness 规则；移除正式快照对设备和同步会话的必需依赖；制定旧字段兼容方案。
- 交付：共享 Schema、OpenAPI、前端生成类型、契约变更记录和状态规则测试。
- 验收：无同步会话的项目可合法进入 `ready`；非法枚举、时间范围和跨项目引用被拒；旧项目可读取或获得明确迁移错误。
- 责任目录：`backend/app/schema.py`、`contracts/`、前端生成类型及契约测试。依赖 L00。

### L02：本地视频导入服务

- 工作：将现有导入提升为正式服务；支持一次选择多个视频并逐文件创建任务；计算内容哈希、项目内去重、空间检查、原子落盘、失败清理和幂等重试。
- 交付：本地导入 API/服务、逐文件结果、集成测试和资源限制说明。
- 验收：空文件、超限、重复内容、上传中断、磁盘不足和幂等重试有确定结果；单文件失败不抹去同批成功文件；客户端路径不能控制服务端读写位置。
- 责任目录：`backend/app/api/`、导入服务、存储与测试。依赖 L01。

### L03：媒体校验与分析副本

- 工作：完整解码校验、格式和投影检查、可播放副本、长视频分段、源时间映射、缓存复用及原片保留策略；明确音轨是否参与分析。
- 交付：媒体服务、映射格式、支持格式说明和测试记录。
- 验收：截断、无视频轨、零时长、不可解码或投影不支持的输入不能完成；分段覆盖完整源时长且可定位播放；重试不重复生成有效副本。
- 责任目录：`backend/app/media/`、媒体 fixtures 与测试。依赖 L01/L02。

### L04：目标规划与视频观察

- 工作：继续支持目标到可观察分镜标准；按确认清单观察所有分析分段；输出动作、结果、质量问题、不确定性和候选证据；记录模型、提示词版本、耗时、用量及失败。
- 交付：规划及分析服务、模型适配器、脱敏样例、提示词版本和模型评估报告。
- 验收：支持多视频、多分段和多对多证据；不根据文件名推断内容；不编造越界证据；模型失败可从缓存副本重试，无需重新上传原视频。
- 责任目录：`backend/app/analysis/`、模型测试和评估资料。依赖 L01/L03。

### L05：覆盖规则、建议与完成判断

- 工作：按必要标准累计证据、重算状态、选择最高优先级缺口并生成单条补拍建议；实现脱离相机会话的 `not_ready/checking/ready` 和完成时间。
- 交付：纯规则模块、建议模板、状态迁移测试和项目快照聚合。
- 验收：未导入素材时显示缺镜建议；处理和阻塞失败阻止最终完成；坏视频不推翻已有好证据；排除素材后正确回退；全部处理完成且标准覆盖时进入 `ready`。
- 责任目录：`backend/app/coverage/`、`backend/app/service.py` 及规则测试。依赖 L01/L04。

### L06：本地视频前端流程

- 工作：保留项目创建、清单生成/编辑/确认和三栏工作区；把本地视频导入改为主操作；支持多选、待上传列表、逐文件进度、失败重试、排除/重新纳入和证据播放；移除相机及同步界面。
- 交付：可运行页面、API client、样式、组件测试和浏览器测试。
- 验收：无需相机即可从新建项目走到分析结果并连续追加视频；失败有恢复入口；界面不出现“连接相机”“最新扫描”“同步会话”或“只接收之后的新片段”。
- 责任目录：`frontend/`。依赖 L01，可使用 fixtures 与 L02—L05 并行开发。

### L07：任务恢复与完整集成

- 工作：整合 API、持久 worker、媒体和模型任务；注入上传中断、进程退出、磁盘不足、媒体失败、模型超时和过期结果；验证恢复、幂等及项目修订隔离。
- 交付：端到端本地视频流程、故障回归、缺陷记录和性能基线。
- 验收：重启后任务恢复；模型重试复用副本；旧任务不能覆盖新素材版本；多视频导入时快照一致；相关测试通过。
- 责任目录：`backend/app/jobs/`、`backend/app/api/`、集成测试和跨模块修复。依赖 L02—L06。

### L08：验收、兼容迁移与相机路径清理

- 工作：按最终流程验收；验证旧数据迁移；从正式启动、健康检查、页面和默认测试中移除 X5；根据 L00 决定删除或归档相机适配代码和专属测试。
- 交付：验收报告、迁移说明、清理清单、模型用量和支持环境记录。
- 验收：未连接相机时完整流程可用；正式页面和 README 不要求相机；旧数据无静默丢失；历史探测证据可查；删除代码无残留引用。
- 责任范围：跨模块清理、迁移和验收。依赖 L07。

### L09：安装与使用交付

- 工作：更新依赖锁定、配置示例、启动/停止/升级、数据保留、模型授权、支持格式、资源限制和故障排查说明。
- 交付：README、`.env.example`、用户指南、故障排查和可复现脚本。
- 验收：另一位使用者不连接相机，按文档从安装、新建项目、确认分镜、导入多个视频到查看补拍建议完整走通；重启后数据仍可访问。
- 责任目录：运行脚本和用户文档。依赖 L08。

## 6. 测试与验收重点

- 创建项目、生成并确认非空分镜；未确认清单不能开始正式分析。
- 单文件与多文件导入、重复内容、部分失败、连续追加和幂等重试。
- 完整视频解码、长视频分段、分段边界和源时间映射。
- 支持、缺陷、不确定和无关素材对分镜状态的影响。
- 多段视频分别满足同一分镜的不同标准；连续动作拒绝不连续证据。
- 处理中保持 `checking`；全部完成后依据覆盖进入 `not_ready` 或 `ready`。
- 排除唯一证据素材后覆盖回退，重新纳入后重新分析或复用缓存。
- 模型未授权、未配置、超时、非法结构和重试恢复。
- 服务重启、租约过期、磁盘不足、媒体丢失和旧结果隔离。
- 页面不依赖设备 API；端到端测试在无相机环境完成。

模拟模型只验证程序流程，不能代替真实视觉效果验收。真实模型应使用人工标注的冻结视频集，记录分镜标准、预期证据区间和缺陷类型。准确率门槛在首轮基线后单独冻结，不能只以接口调用成功作为验收。

## 7. 开发协作规则与交付模板

本次只修改计划，不启动实现。共享契约由 L01 维护；各任务按责任范围实施，接口变更同步更新依赖者，合并后运行相关单元、集成和浏览器测试。

每张任务卡采用“确认现状与失败用例 → 最小实现 → 自动检查 → 集成 → 交付”的顺序。不得以页面截图、模拟模型输出或手工修改数据库代替功能验收。不得重新引入相机连接作为本地视频分析的前置条件。

```text
请执行「拍够了吗？」软件开发与使用计划 v2.0 的 Lxx。
先阅读开发计划_初稿.md、docs/架构与接口设计.md、docs/开发测试与验收流程.md、
docs/交接说明.md，以及仓库适用的 AGENTS.md。当前实现授权来自本条执行请求。
产品以用户确认后的成片目标和分镜清单为分析基准，只分析主动导入的本地视频。
任务范围、责任目录、依赖与验收以对应任务卡为准；保护已有数据和无关改动。
先建立有辨识力的失败用例，再完成最小实现，运行相关测试并保留结果。
不得引入相机连接、目录扫描或设备同步；不得用假模型冒充真实视觉验收。
接口变更同步契约、生成类型和依赖者；凭据只从本地配置读取，不写入仓库或日志。
结束时报告：修改文件、执行命令与结果、验收场景、迁移影响、未验证项和已知问题。
```

首轮从 L00 开始，在删除相机代码前完成引用和数据兼容盘点。L01 冻结状态语义后，L02—L06 可按依赖并行推进；L07 统一集成，L08 再清理历史路径。最先需要团队提供普通视角真实视频样本、成片目标及人工期望分镜、模型服务配置与可接受的上传和用量范围。
