# 「拍够了吗？」软件开发与使用计划 v1.0

> 2026-09-22 更新。用户已认可：Mac + X5，首次选择本项目已有素材，此后自动同步新增片段；用户在相机上录制，软件分析分镜覆盖并建议补拍。本轮仅交付设计及执行计划，未开始产品实现或真机测试。
> 本版替代 v0.1/v0.2：自动同步是核心交付要求，不再是可选扩展；不开发网页录制控制。具体 X5 固件、目录枚举能力、媒体处理路径与模型仍需前置验证。

## 1. 文档入口和实施边界

- [架构与模块接口](docs/架构与接口设计.md)：进程、数据、模块职责、服务接口、HTTP API、状态与异常语义。
- [开发测试与验收流程](docs/开发测试与验收流程.md)：分层测试、固定用例、真机门槛、回归与交付记录。
- 本文件：产品范围、实施顺序、agent 任务卡、软件使用交付。

三份文件是一套设计基线。实现阶段以本文件的范围为准，接口细节依第二份文件，验收依第三份文件；变更应同步修改涉及的文档及测试。HTTP 接口的可执行 OpenAPI/Schema 在 T01 生成，此处不声称已实现。

计划只包括软件开发、测试、安装、运行和使用。不包括市场调研、路演、宣传视频制作、实际拍摄组织和设备采购。团队提供测试设备、素材和模型账户，测试中的相机操作属于联调输入。

## 2. 软件使用流程

1. 在 Mac 启动本地服务，浏览器打开应用，创建教程项目，输入主题、目标时长与拍摄条件。
2. 软件生成分镜草稿：准备、必要操作、结果；每个分镜包含可观察的通过标准。用户修改并确认后锁定清单。
3. Mac 连接 X5，软件扫描卡内目录，展示素材列表。用户勾选本项目已有片段，或选择「只接收之后的新片段」。首次只列目录和可取得的轻量信息，不自动分析整张卡。
4. 用户确认本项目同步范围；软件导入选中素材并分析。一个分镜可以关联多段证据，一段视频可以覆盖多个分镜。
5. 用户继续在 X5 上拍摄；软件定期检查目录，发现新文件，确认文件组就绪，下载、预处理、分析，自动更新分镜覆盖。
6. 页面展示分镜清单、素材及证据、当前优先建议，例如「连接处被手遮挡，从侧上方补拍安装动作」。
7. 所有必要分镜具备合格证据且同步检查完成后，显示「已拍够」及检查时间；网络异常或仍有任务时，保留已有进度并显示尚未核验部分。
8. 用户结束本次同步会话，保留项目、素材和证据。重新进入时恢复历史；重连间隔内新增片段先展示给用户确认，再继续自动同步。

页面以三个区块为主：分镜进度、素材播放器、当前建议。顶部单独显示相机连接/扫描、下载/分析、模型网络状态。证据点击后在分析所用的同一视频副本定位播放。

## 3. 首版范围与成功定义

### 必须交付

- Mac 本地运行：React + TypeScript + Vite，Python + FastAPI，SQLite，本地媒体存储，FFmpeg/ffprobe。
- 一台 X5、一个活动同步项目；可保存多个项目，切换前结束旧同步会话。
- 已有素材目录浏览与选择，新增素材发现、文件组识别、稳定性确认、去重和恢复。
- 导入 → 分析 → 分镜匹配 → 状态重算 → 单条补拍建议的完整链路。
- 证据定位、模型输出校验、持久任务、失败恢复、安装和使用文档。
- 本地文件导入作为调试及备用入口，但不能替代 X5 自动同步的核心验收。

### 不在首版范围

GO 3S/Android 原生客户端、相机录制控制、实时视频流分析、自动剪辑、任意全景原片自动处理、多人协作、账户支付、公网部署、相机文件删除与固件升级。

30—60 秒是目标成片的内容规划范围，不是相机源文件长度限制。源文件、磁盘与模型输入上限分开配置：大原片可保留，分析采用派生副本或分段；超限必须显示原因，不能只分析前半段却把整段标为完成。

## 4. 两个前置技术门槛

### G0：X5 能否满足自动同步

验证必须以「用户在相机上录制」为操作方式，包括已有视频分页枚举、录制结束后的新增发现、文件组完整性、录制中能否枚举/下载、断连后补扫和 Mac 外网访问。

官方 OSC 文档展示了照片列表及停止录像返回下载地址，不能据此假定已证明任意历史视频枚举和硬件按钮录制后的新增发现。此前飞书使用说明仍未能读取。实现者须获取可读说明并记录实际接口、参数、固件与响应；不得凭照片示例编造视频分页能力。[官方 OSC API](https://insta360develop.github.io/Insta360-Developer_Docs/ch/x/osc/api/)

通过后才能冻结真实 CameraAdapter。若不能满足，保留已完成的软件模块，提交替代传输路径及差异供讨论；不静默改为网页控制录制，也不以手动导入宣布交付完成。

### G1：下载文件能否成为可靠的分析输入

验证 X5 指定模式输出的编码、投影方式、分段/多文件结构、时长与播放。优先找可直接形成普通视角视频的已验证模式；不提前承诺具体模式支持。

若必须借助外部软件导出，则将其标为人工步骤，该路径只能验证后续分析模块，不能满足完整自动链路验收。要交付全自动，必须验证可在目标环境自动运行的媒体处理方案，另行估时并更新范围。仅改后缀、转封装或把双鱼眼帧交给模型均不算解决。

桌面 SDK 公开平台为 Windows/Linux，不能默认在 Mac 可用。[官方桌面说明](https://insta360develop.github.io/Insta360-Developer_Docs/ch/x/desktop/guide/)

## 5. 实施顺序和任务卡

任务编号在本版重新定义，旧版任务卡作废。每项交付都须附测试结果、未验证项和下一任务需要的接口。没有真机时可完成模拟器与业务模块，但 G0/G1 和最终真机验收保持未完成。

| 阶段 | 内容 | 出口 |
|---|---|---|
| A | T00 设备与媒体探测；T01 基础契约 | 确定真实接入是否可行，不押后硬件风险 |
| B | T02 存储调度；T03 同步；T04 媒体；T05 分析；T07 页面 | 模块可由模拟输入驱动，接口一致 |
| C | T06 规则建议，T08 完整集成 | 自动导入到补拍建议走通 |
| D | T09 真机验收；T10 运行交付 | 实际软件可用且可复现 |

### T00：设备与媒体可行性探测

- 输入：本套文档、实际 Mac/X5、固件、团队提供的原片和可读官方说明。
- 工作：验证 G0/G1；记录卡内多页目录、相机按钮录制、录像中与结束后目录、下载及联网行为；选择最低复杂度的真实传输/媒体路径。
- 交付：`docs/device-probe.md`、脱敏原始响应、真实媒体检查报告、支持/不支持/未验证能力表。
- 验收：测试文档中的 HW01—HW06 有实测结果；不能只证明连接成功或停止指令有返回。
- 责任目录：`tools/probes/`、`tests/fixtures/device/`、探测报告。依赖：无；模型探测可稍后补充。

### T01：冻结契约并建立测试骨架

- 输入：架构、测试流程；T00 逐步补充的能力证据。
- 工作：定义共享 Schema、OpenAPI、错误码、持久状态枚举；建立 Camera/Model 假实现和样例；设置类型检查、单元测试及浏览器测试入口。
- 交付：`contracts/`、`tests/fixtures/`、`docs/contracts-changelog.md`、统一运行脚本。
- 验收：合法样例通过、错误 ID/时间段/枚举被拒；前端使用生成类型；未来的实现命令可运行。真实相机契约在 T00 后冻结。
- 责任目录：共享契约、测试基础配置。依赖：业务部分无；真实相机能力字段依赖 T00。

### T02：持久化和任务调度

- 输入：T01。
- 工作：数据库迁移、仓储、磁盘目录、原子任务入队、工作进程生命周期、租约/恢复、项目修订号与一致快照。
- 交付：存储及 worker 模块、临时库集成测试、重启恢复记录。
- 验收：重复入队只产生一个有效任务；过期结果不能覆盖当前数据；进程中断后可恢复；文件路径不由用户任意指定。
- 责任目录：`backend/app/storage/`、`backend/app/jobs/`；依赖 T01。

### T03：相机目录与增量同步

- 输入：T00 的真实能力、T01 的 CameraAdapter、T02。
- 工作：连接、完整目录扫描、首次范围确认、轮询、文件组就绪、下载、基线/去重、暂停和重连补扫。
- 交付：真实适配器、同步服务、模拟相机脚本与同步集成测试。
- 验收：SY01—SY10；同一未变片段不反复分析，未就绪文件不入队，断连新增素材不跨项目自动归属。
- 责任目录：`backend/app/camera/`、`backend/app/sync/`；模拟实现可在 T00 前开发，真实验收依赖 T00/T02。

### T04：媒体校验与分析副本

- 输入：T00 文件类型结论、T01、T02、真实样本。
- 工作：媒体检测、支持模式判定、生成浏览器可播放副本、分段与时间映射、资源限制、原文件组保留。
- 交付：媒体服务、元数据与映射格式、媒体测试记录。
- 验收：ME01—ME05；截断文件、缺少组成员、未处理全景输入均不被标为 ready。
- 责任目录：`backend/app/media/`；依赖 T01/T02，真实路径依赖 G1。

### T05：目标规划与视频观察

- 输入：T01、已选模型访问方式、T04 分析副本。
- 工作：目标到可观察分镜标准；视频/抽帧观察；分镜与证据匹配；校验输出；记录模型/提示词版本、耗时、用量及失败。
- 交付：ModelAdapter、规划及分析服务、脱敏样例、冻结评估集与模型报告。
- 验收：AI01—AI06；支持多对多关联，不凭文件名推断内容，不编造时间证据；置信度不单独决定完成。
- 责任目录：`backend/app/analysis/`；依赖 T01；模拟开发可独立，真实评估依赖 T04/模型配置。

### T06：覆盖规则、建议与完成判断

- 输入：T01、T02、T05 的结构化观察；同步状态契约。
- 工作：逐通过标准累计证据，重算分镜状态，选择下一建议，生成带修订号和检查时间的完成依据。
- 交付：纯规则模块、建议模板及可选模型润色、规则测试。
- 验收：RU01—RU08；差新片不推翻旧好证据；断连不抹去覆盖，但不能显示已核验最新素材；移除素材后正确回退。
- 责任目录：`backend/app/coverage/`；依赖 T01/T02；可先用 fixtures，联调依赖 T05。

### T07：前端使用流程

- 输入：T01 与快照 fixtures。
- 工作：目标/清单确认、设备与素材范围选择、三栏工作区、进度/错误提示、播放器证据、暂停恢复、排除误收片段。
- 交付：可运行页面、API client、组件与浏览器测试。
- 验收：UI01—UI06；无网页录制按钮；过期快照不覆盖新状态；失败有明确恢复入口；模拟模式明确可见。
- 责任目录：`frontend/`；依赖 T01，可与后端并行。

### T08：真实服务集成与故障回归

- 输入：T02—T07。
- 工作：整合 API/worker、自动同步到分析的端到端链路；注入网络、进程、磁盘和模型失败并验证恢复。
- 交付：可运行集成版、E2E 脚本、缺陷记录。
- 验收：E01—E05 与恢复测试通过；失败记录保留，修复后跑相关回归，不能把 live 测试替换为 mock 测试。
- 责任目录：`backend/app/api/`、集成测试、必要跨模块修复；依赖全部模块交付。

### T09：X5 真机与模型验收

- 输入：T08、实际 X5/Mac、冻结样例及模型。
- 工作：按用户使用方式测试已有素材选择、相机直接录制新片、自动发现和分析、补拍及完成判断；记录实际性能。
- 交付：`docs/acceptance-report.md`、运行日志、逐步截图/状态记录、实际模型用量和支持环境。
- 验收：测试文档的最终出口全部通过；手动步骤如存在必须披露，G0/G1 未过则整体未完成。
- 责任目录：验收记录和修复；依赖 T08、G0/G1。

### T10：安装与使用交付

- 输入：T09 合格版本。
- 工作：提供依赖锁定、配置示例、启动/停止/升级与数据保留说明、日常操作与故障排查指南。
- 交付：README、`.env.example`、`docs/user-guide.md`、`docs/troubleshooting.md`、可复现运行脚本。
- 验收：另一位使用者在约定 Mac 环境按文档从安装到完整使用走通；重启后项目与本地证据仍可访问。
- 责任目录：运行脚本、用户文档；依赖 T09。

## 6. 开发协作规则与交付模板

后续可由不同 agent 执行独立任务。本轮不启动开发代理。共享契约由 T01 负责人维护；不同 agent 负责不同目录，按依赖交付，独立分支/worktree 合并后再跑集成测试。

每张任务卡分为「接口与失败用例 → 最小实现 → 自动检查 → 集成 → 交付」。不以已写代码或页面截图替代功能验收。设备缺失时完成可独立验证的工作，保留硬件验收为待办。

可复制的执行提示：

```text
请执行「拍够了吗？」开发计划 v1.0 的 Txx。
先阅读开发计划_初稿.md、docs/架构与接口设计.md、docs/开发测试与验收流程.md，
以及仓库适用的 AGENTS.md。当前任务的具体实现授权来自本条执行请求。
任务范围、责任目录、依赖与验收以对应任务卡为准；报告尚未满足的依赖。
先写有辨识力的失败用例，再完成最小实现，运行相关测试并保留证据。
不得自行改为程序控制录制，不得用手动导入或假模型冒充自动同步交付。
接口变更要同步契约和依赖者；凭据仅从本地配置读取，不写入日志或仓库。
结束时报告：修改文件、实际执行命令与结果、验收用例 ID、未验证项、已知问题和交接说明。
```

工期按阶段出口推进，T00/T05 得到结果后再估具体日期；不沿用旧版对网页控制录制路线的小时估计。最先需要团队提供：X5 固件及模式、Mac 版本/芯片、真实原片、模型服务名称与预算、软件交付期限。飞书说明如仍不可读，记录缺项，不阻断无关模块开发，也不编造其要求。
